[package]
name = "kajet-parser"
version = "0.2.0"
edition = "2024"

[dependencies]
ignore = "0.4"
pulldown-cmark = "0.13"
anyhow = "1"
regex = "1"
unicode-normalization = "0.1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
tempfile = "3"
